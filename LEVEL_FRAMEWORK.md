# 임원 AI 사용 현황 — 사용자 레벨 분석 프레임워크

> **4단계: 자연인 → 체험자 → 활용자 → 달인**
> 설문 응답 8개 문항을 점수화해 각 사용자를 4개 레벨 중 하나로 판정한다.

---

## 1. 4단계 정의

| 레벨 | 명칭 | 점수 | 설명 |
|------|------|------|------|
| **L1** | 🍃 자연인 | ~2.5 | 아직 AI를 업무에 안 쓰는 '그대로의 나'. 계기만 생기면 시작하기 좋은 단계 |
| **L2** | 🔍 체험자 | 2.5~5.5 | 도구를 써 보았으나 개인·간헐적. 관심은 있으나 업무에 체계적으로 녹이기 전 |
| **L3** | ⚙️ 활용자 | 5.5~8.5 | 도구를 일상 업무에 꾸준히 사용. 자료입력·허용범위 확장으로 달인급 진입 가능 |
| **L4** | 🏆 달인 | 8.5~ | 기업 AI 전도사. 여러 도구를 전략적으로 결합해 업무를 재설계하고 동료에게 권함 |

---

## 2. 문항별 배점

| 문항 | 답변 → 점수 |
|------|------|
| **도구(tools)** | 사용 도구 1개당 +1.5 / '사용하지 않음' -3 |
| **유료(paid)** | 회사/개인 유료 +2 / 무료만 +0.5 |
| **빈도(freq)** | 하루 여러 번 +3 / 하루 1회 +2 / 주2~3회 +1.5 / 주1회미만 +0.5 |
| **용도(use)** | 문서작성 +1 / 조사·회의록·요약·데이터분석 +1.5~2 (복수 가산) |
| **에이전트(agent)** | 직접 쓴다 +3 / 개념 안다 +1.5 / 들어봤다 +0.5 |
| **허용(trust)** | 초안만 +1 / 내부자료 +2 / 문서작성·저장 +3 / 메일 등 외부행위 +4 |
| **자료입력(data)** | 있다 +2 / 민감X만 +1.5 / 없다 0 |
| **유료제공(support)** | 적극 +3 / 사용할 것 +2 / 모르겠다 +1 |

**총 상한 ≈ 16+ 점**.

---

## 3. 판정 로직

```python
def level_for(total):
    if total >= 8.5: return "달인"
    if total >= 5.5: return "활용자"
    if total >= 2.5: return "체험자"
    return "자연인"
```

- **달인→활용자 지표**: 도구 수, 허용범위(2→3단계), 자료입력 유무, 에이전트 실사용
- 핵심 증가요인: trust(허용)의 3~4단계 + agent(에이전트 직접 사용) + data(자료입력)

---

## 4. 집계 후 먼저 볼 것 (설계 의도와 연결)

1. **레벨 분포** — 자연인이 많으면 "교육 우선", 활용자~달인이 많으면 "정책/권한 설계 우선"
2. **개인 유료 비율** → 회사 유료 지원의 정당성 근거
3. **자료입력 '있다' 비율** → 보안 가이드라인 시급성
4. **달인/활용자를 멘토로** → 사내 AI 커뮤니티·가이드 제작 주체

---

## 5. 실행

```bash
cd C:\Users\ajina\ai-survey_deploy
python level_analysis.py        # 시트 응답 → 사용자별 레벨/분포 출력
```

- 데이터 출처: Vercel 설문(byucksan-ai-survey) → 구글시트 댓글 JSON → 본 스크립트가 파싱
- 실사용 응답이 쌓일수록 분포가 의미를 갖는다 (현재 시트엔 테스트 데이터 위주)

---

*레벨 분류 로직: `level_analysis.py` (이 문서와 함께 배치)*